import React from 'react'
import { ComponentProps } from '../../types/props'

function MapWidget({element}: ComponentProps) {
    return (
        <div>
            chat widget
        </div>
    )
}

export default MapWidget
