import React from 'react'
import { ComponentProps } from '../../types/props'

function RateWidget({ element }: ComponentProps) {
    return (
        <div>
            chat widget
        </div>
    )
}

export default RateWidget
